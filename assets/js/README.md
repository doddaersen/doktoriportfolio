# Portfolio JavaScript

The current JavaScript still lives inline in `index.html`.

It should be moved here only after the full inline script can be read without truncation. The GitHub connector currently truncates the long `index.html` response near the script block, so this file is intentionally a placeholder for the next safe refactor step.

Planned target file:

- `assets/js/portfolio-status.js`

Do not remove the inline script from `index.html` until the complete current script has been verified.